<view class="field-number">
  <view class="field-label"><text wx:if="{{required}}" class="required">*</text><text>{{label}}</text></view>
  <input class="input" type="number" value="{{inputValue}}" placeholder="{{placeholder}}" bindinput="onInput" />
</view>
