<view class="field-input">
  <view class="field-label">
    <text wx:if="{{required}}" class="required">*</text>
    <text>{{label}}</text>
  </view>
  <input class="input" type="text" placeholder="{{placeholder}}" bindinput="onInput" />
</view>
